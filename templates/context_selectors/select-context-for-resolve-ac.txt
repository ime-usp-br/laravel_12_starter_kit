**Sua Tarefa ÚNICA e ABSOLUTAMENTE RESTRITA:**
Com base na descrição da tarefa principal e no **CONTEXTO FORNECIDO** (que inclui CONTEÚDO ESSENCIAL e SUMÁRIOS de outros arquivos), selecione e **PRIORIZE** os arquivos mais relevantes para a tarefa principal.

**Tarefa Principal:** Gerar o código PHP/Blade/JS/CSS/SQL (incluindo testes, traduções, migrations, etc.) estritamente necessário para resolver o Critério de Aceite (AC) número `__NUMERO_DO_AC__` da Issue GitHub `__NUMERO_DA_ISSUE__`, considerando a observação `__OBSERVACAO_ADICIONAL__` e o plano geral da Issue.

**Contexto Fornecido:**
1.  **Conteúdo Essencial Pré-injetado:** Você receberá o conteúdo completo (ou sumário/truncado) de arquivos críticos como `github_issue___NUMERO_DA_ISSUE___details.json` e diffs recentes (`git_diff_*.txt`). Analise este conteúdo diretamente para entender os requisitos e o estado atual.
2.  **Sumários de Outros Arquivos:** Você receberá uma lista de sumários/metadados de outros arquivos (path, type, summary, token_count), incluindo código fonte, testes, guias, configurações, etc. Use estas informações para decidir se o arquivo completo correspondente deve ser incluído.

**Seu Processo de Seleção Priorizada:**
1.  Entenda profundamente o objetivo da **Tarefa Principal**: implementar o AC `__NUMERO_DO_AC__` da Issue `__NUMERO_DA_ISSUE__`.
2.  Analise o **Conteúdo Essencial Pré-injetado** (detalhes da issue, diffs).
3.  Analise os **Sumários de Outros Arquivos**.
4.  Crie uma **LISTA PRIORIZADA** de caminhos relativos (`path`) dos arquivos que você considera MAIS importantes para a IA gerar o código para o AC. A ordem é crucial.
5.  **Arquivos a Priorizar (em ordem aproximada de importância):**
    *   O arquivo de detalhes da Issue (`github_issue___NUMERO_DA_ISSUE___details.json` - conteúdo pré-injetado).
    *   Arquivos de código fonte (`.php`, `.blade.php`, `.js`) que **serão modificados ou criados** para o AC (identifique pelos sumários e descrição do AC).
    *   Código fonte **relacionado** (Models, Services, Traits, Interfaces usados ou referenciados) (identifique pelos sumários e `dependencies`).
    *   Arquivos de teste (`tests/`) existentes relacionados ou que precisarão ser criados/atualizados (identifique pelos sumários).
    *   Arquivos de tradução (`lang/*.json`) se o AC envolve texto de UI.
    *   Arquivos de configuração (`config/*.php`) relevantes para o AC.
    *   Guias de desenvolvimento (`guia_*.md`, `padroes_*.md`).
    *   Diffs recentes (`git_diff_*.txt` - conteúdo pré-injetado).
6.  **Objetivo de Tokens:** Tente manter o **soma** dos `token_count` dos arquivos que você selecionou (usando o valor do sumário/metadados) abaixo de aproximadamente `__BUDGET_PARA_CHAMADA_PRINCIPAL__` tokens. **PRIORIZE incluir os detalhes da issue, o código a ser modificado/criado, código relacionado e testes**, mesmo que exceda um pouco o budget.
7.  **Exclua:** Código não relacionado, dependências, logs genéricos, arquivos de contexto de outras issues, a menos que o `summary` indique relevância crítica para a implementação *deste* AC.

**Formato de Saída OBRIGATÓRIO E ESTRITO:**
Sua resposta DEVE ser **APENAS E SOMENTE APENAS** um objeto JSON válido contendo UMA ÚNICA chave chamada `relevant_files`. O valor desta chave DEVE ser a LISTA **PRIORIZADA** (array JSON) de strings, onde cada string é o caminho relativo EXATO de um arquivo selecionado.

**Exemplo de Saída:**
```json
{
  "relevant_files": [
    "context_llm/code/YYYYMMDD_HHMMSS/github_issue___NUMERO_DA_ISSUE___details.json",
    "context_llm/code/YYYYMMDD_HHMMSS/git_diff_cached.txt",
    "app/Http/Controllers/TargetController.php",
    "app/Services/RelatedService.php",
    "resources/views/target/view.blade.php",
    "tests/Feature/TargetFeatureTest.php",
    "docs/padroes_codigo_boas_praticas.md",
    "lang/en.json"
  ]
}
```

**REGRAS ADICIONAIS:**
-   **NÃO** inclua NENHUM texto explicativo.
-   Sua resposta completa DEVE começar com `{` e terminar com `}`.
-   Se NENHUM arquivo for considerado relevante (altamente improvável), retorne `{"relevant_files": []}`.