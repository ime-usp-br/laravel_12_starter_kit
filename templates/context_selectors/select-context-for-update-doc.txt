**Sua Tarefa ÚNICA e ABSOLUTAMENTE RESTRITA:**
Com base na descrição da tarefa principal e no **CONTEXTO FORNECIDO** (que inclui CONTEÚDO ESSENCIAL e SUMÁRIOS de outros arquivos), selecione e **PRIORIZE** os arquivos mais relevantes para a tarefa principal.

**Tarefa Principal:** Analisar as mudanças da Issue `__NUMERO_DA_ISSUE__` e gerar o conteúdo atualizado para o arquivo de documentação `__ARQUIVO_DOC_ALVO__`, e potencialmente o `CHANGELOG.md`.

**Contexto Fornecido:**
1.  **Conteúdo Essencial Pré-injetado:** Você receberá o conteúdo completo (ou sumário/truncado) de arquivos críticos como `github_issue___NUMERO_DA_ISSUE___details.json`, diffs (`git_diff_*.txt`), e o conteúdo atual do arquivo `__ARQUIVO_DOC_ALVO__`. Analise este conteúdo diretamente.
2.  **Sumários de Outros Arquivos:** Você receberá uma lista de sumários/metadados de outros arquivos (path, type, summary, token_count), incluindo `CHANGELOG.md`, guias de documentação, etc. Use estas informações para decidir se o arquivo completo correspondente deve ser incluído.

**Seu Processo de Seleção Priorizada:**
1.  Entenda profundamente o objetivo da **Tarefa Principal**: atualizar `__ARQUIVO_DOC_ALVO__` e `CHANGELOG.md` para refletir a Issue `__NUMERO_DA_ISSUE__`.
2.  Analise o **Conteúdo Essencial Pré-injetado** (issue, diffs, doc alvo).
3.  Analise os **Sumários de Outros Arquivos**.
4.  Crie uma **LISTA PRIORIZADA** de caminhos relativos (`path`) dos arquivos que você considera MAIS importantes para a IA gerar a documentação atualizada. A ordem é crucial.
5.  **Arquivos a Priorizar (em ordem aproximada de importância):**
    *   O arquivo de detalhes da Issue (`github_issue___NUMERO_DA_ISSUE___details.json` - conteúdo pré-injetado).
    *   Os arquivos de diff (`git_diff_*.txt` - conteúdo pré-injetado).
    *   O arquivo de documentação alvo (`__ARQUIVO_DOC_ALVO__` - conteúdo pré-injetado).
    *   O arquivo `CHANGELOG.md` (identifique pelo sumário).
    *   O guia de versionamento da documentação (`docs/versionamento_documentacao.md` - use o sumário).
    *   Outros guias (`guia_*.md`, `padroes_*.md`) para referência de estilo (use sumários).
6.  **Objetivo de Tokens:** Tente manter o **soma** dos `token_count` dos arquivos que você selecionou (usando o valor do sumário/metadados) abaixo de aproximadamente `__BUDGET_PARA_CHAMADA_PRINCIPAL__` tokens. **PRIORIZE incluir os detalhes da issue, os diffs e o conteúdo atual dos documentos alvo (`__ARQUIVO_DOC_ALVO__`, `CHANGELOG.md`)**, mesmo que exceda um pouco o budget.
7.  **Exclua:** Código fonte, testes, configurações, dependências, outros arquivos de contexto não diretamente necessários para *entender as mudanças e reescrever a documentação alvo*.

**Formato de Saída OBRIGATÓRIO E ESTRITO:**
Sua resposta DEVE ser **APENAS E SOMENTE APENAS** um objeto JSON válido contendo UMA ÚNICA chave chamada `relevant_files`. O valor desta chave DEVE ser a LISTA **PRIORIZADA** (array JSON) de strings, onde cada string é o caminho relativo EXATO de um arquivo selecionado.

**Exemplo de Saída:**
```json
{
  "relevant_files": [
    "context_llm/code/YYYYMMDD_HHMMSS/github_issue___NUMERO_DA_ISSUE___details.json",
    "context_llm/code/YYYYMMDD_HHMMSS/git_diff_cached.txt",
    "__ARQUIVO_DOC_ALVO__",
    "CHANGELOG.md",
    "docs/versionamento_documentacao.md",
    "README.md"
  ]
}
```

**REGRAS ADICIONAIS:**
-   **NÃO** inclua NENHUM texto explicativo.
-   Sua resposta completa DEVE começar com `{` e terminar com `}`.
-   Se NENHUM arquivo for considerado relevante (improvável), retorne `{"relevant_files": []}`.